FROM python:3.11-slim

WORKDIR /app

# Install dependencies
COPY ami-handler-requirements.txt requirements.txt
RUN pip install --no-cache-dir -r requirements.txt

# Copy the handler script
COPY ami-auto-answer-handler.py .

# Run the handler
CMD ["python", "-u", "ami-auto-answer-handler.py"]
